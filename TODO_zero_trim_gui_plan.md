# TODO: Поэтапное обновление GUI для эксплуатационной подгонки 0% (`zero_trim`)

## Цель
Добавить в GUI отдельный, понятный и безопасный рабочий процесс для коррекции смещения 0% по фактическому выходу МК, без изменения `k1` и базовой температурной модели.

---

## Этап 0. Подготовка и совместимость
- [ ] Проверить, что в МК доступны DID чтения/записи `fuel_zero_trim_count`.
- [ ] Добавить константы DID в общий контракт UDS GUI.
- [ ] Проверить, что режимы безопасности/сессии UDS позволяют запись параметра.
- [ ] Подготовить текстовые статусы и подсказки для оператора (простым языком).

---

## Этап 1. Модель данных и controller-слой

### 1.1 Свойства состояния
- [ ] Добавить свойства controller для `zero_trim`:
  - [ ] текущее значение,
  - [ ] рекомендованное значение,
  - [ ] дельта коррекции,
  - [ ] статус операции,
  - [ ] флаг занятости/прогресса.
- [ ] Добавить сигналы обновления состояния для QML.

### 1.2 UDS-операции
- [ ] Реализовать чтение `zero_trim` из МК.
- [ ] Реализовать запись `zero_trim` в МК с проверкой диапазона.
- [ ] Реализовать сброс `zero_trim` в 0.
- [ ] Добавить таймауты/повторы и понятные сообщения об ошибках.

---

## Этап 2. UI-блок эксплуатационной подгонки

### 2.1 Размещение на форме
- [ ] Добавить отдельный блок/спойлер «Эксплуатационная подгонка 0%» в карточке Калибровка.
- [ ] Вынести блок визуально отдельно от офлайн-анализа CSV.

### 2.2 Элементы управления
- [ ] Кнопка «Прочитать zero trim».
- [ ] Кнопка «Подогнать 0% по факту».
- [ ] Кнопка «Записать zero trim».
- [ ] Кнопка «Сбросить zero trim».
- [ ] Поля: «Текущее», «Рекомендованное», «Δ смещения», «Остаток ошибки».
- [ ] Прогресс/индикатор выполнения операций.

### 2.3 UX-требования
- [ ] Никаких лишних полей и дублей, минималистичный интерфейс.
- [ ] Все подписи на русском, в одном стиле.
- [ ] Явно показать, что `k1/k0/empty/full` при этой операции не меняются.

---

## Этап 3. Алгоритм автоподгонки по факту МК

### 3.1 Входные данные
- [ ] Брать текущее топливо/период/температуру из МК (онлайн), а не из CSV.
- [ ] Перед расчетом собирать окно усреднения (например 10–30 секунд).
- [ ] Проверять стабильность сигнала (разброс ниже порога).

### 3.2 Расчет
- [ ] Рассчитать требуемый `delta_count` для приведения к 0%.
- [ ] Сформировать `zero_trim_new = zero_trim_current + delta_count`.
- [ ] Ограничить значение безопасным диапазоном.
- [ ] Отдельно показывать оператору рассчитанный шаг и итог.

### 3.3 Верификация
- [ ] После записи автоматически перечитать параметры.
- [ ] Проверить остаточную ошибку 0%.
- [ ] Выдать статус «успех/нужно повторить/подозрение на механику».

---

## Этап 4. Интеграция с текущими сценариями
- [ ] После изменения калибровок 0%/100% показывать напоминание о подгонке `zero_trim`.
- [ ] Не смешивать рекомендации CSV (офлайн) и онлайн-подгонку в одном действии.
- [ ] В режиме офлайн-анализа оставлять расчет `k1` отдельно от эксплуатационного `zero_trim`.

---

## Этап 5. Логирование и экспорт
- [ ] Логировать операции подгонки: время, SA, температура, старый/new `zero_trim`, остаток.
- [ ] Добавить запись этих данных в CSV-лог сессии (опционально отдельный файл).
- [ ] Подготовить короткую сводку для сервисного отчета.

---

## Этап 6. Тестирование GUI
- [ ] Unit/functional проверки controller-слоя на чтение/запись DID.
- [ ] Проверка сценария «нормальная подгонка 0%».
- [ ] Проверка ошибок связи/таймаутов.
- [ ] Проверка UI на разных размерах окна (без разъезда компонентов).
- [ ] Проверка кодировки UTF-8 во всех новых строках и файлах.

---

## Критерии готовности
- [ ] Оператор может подогнать 0% в 3-4 действия без ручной математики.
- [ ] Коррекция меняет только `zero_trim`.
- [ ] После записи и верификации значение стабильно в допуске.
- [ ] Интерфейс понятный, без перегруженности и без блокировок UI.