# TODO: Поэтапное обновление GUI для эксплуатационной подгонки 0% (`zero_trim`)

## Цель
Добавить в GUI отдельный, понятный и безопасный рабочий процесс для коррекции смещения 0% по фактическому выходу МК, без изменения `k1` и базовой температурной модели.

---

## Этап 0. Подготовка и совместимость
- [x] Проверить, что в МК доступны DID чтения/записи `fuel_zero_trim_count`.
- [x] Добавить константы DID в общий контракт UDS GUI.
- [x] Проверить, что режимы безопасности/сессии UDS позволяют запись параметра.
- [x] Подготовить текстовые статусы и подсказки для оператора (простым языком).

---

## Этап 1. Модель данных и controller-слой

### 1.1 Свойства состояния
- [x] Добавить свойства controller для `zero_trim`:
  - [x] текущее значение,
  - [x] рекомендованное значение,
  - [x] дельта коррекции,
  - [x] статус операции,
  - [x] флаг занятости/прогресса.
- [x] Добавить сигналы обновления состояния для QML.

### 1.2 UDS-операции
- [x] Реализовать чтение `zero_trim` из МК.
- [x] Реализовать запись `zero_trim` в МК с проверкой диапазона.
- [x] Реализовать сброс `zero_trim` в 0.
- [ ] Добавить таймауты/повторы и понятные сообщения об ошибках.

---

## Этап 2. UI-блок эксплуатационной подгонки

### 2.1 Размещение на форме
- [x] Добавить отдельный блок/спойлер «Эксплуатационная подгонка 0%» в карточке Калибровка.
- [x] Вынести блок визуально отдельно от офлайн-анализа CSV.

### 2.2 Элементы управления
- [x] Кнопка «Прочитать zero trim».
- [x] Кнопка «Подогнать 0% по факту».
- [x] Кнопка «Записать zero trim».
- [x] Кнопка «Сбросить zero trim».
- [x] Поля: «Текущее», «Рекомендованное», «Δ смещения», «Остаток ошибки».
- [x] Прогресс/индикатор выполнения операций.

### 2.3 UX-требования
- [ ] Никаких лишних полей и дублей, минималистичный интерфейс.
- [ ] Все подписи на русском, в одном стиле.
- [x] Явно показать, что `k1/k0/empty/full` при этой операции не меняются.

---

## Этап 3. Алгоритм автоподгонки по факту МК

### 3.1 Входные данные
- [x] Брать текущее топливо/период/температуру из МК (онлайн), а не из CSV.
- [x] Перед расчетом собирать окно усреднения (например 10–30 секунд).
- [x] Проверять стабильность сигнала (разброс ниже порога).

### 3.2 Расчет
- [x] Рассчитать требуемый `delta_count` для приведения к 0%.
- [x] Сформировать `zero_trim_new = zero_trim_current + delta_count`.
- [x] Ограничить значение безопасным диапазоном.
- [x] Отдельно показывать оператору рассчитанный шаг и итог.

### 3.3 Верификация
- [x] После записи автоматически перечитать параметры.
- [x] Проверить остаточную ошибку 0%.
- [x] Выдать статус «успех/нужно повторить/подозрение на механику».

---

## Этап 4. Интеграция с текущими сценариями
- [x] После изменения калибровок 0%/100% показывать напоминание о подгонке `zero_trim`.
- [x] Не смешивать рекомендации CSV (офлайн) и онлайн-подгонку в одном действии.
- [x] В режиме офлайн-анализа оставлять расчет `k1` отдельно от эксплуатационного `zero_trim`.

---

## Этап 5. Логирование и экспорт
- [x] Логировать операции подгонки: время, SA, температура, старый/new `zero_trim`, остаток.
- [ ] Добавить запись этих данных в CSV-лог сессии (опционально отдельный файл).
- [x] Подготовить короткую сводку для сервисного отчета.

---

## Этап 6. Тестирование GUI
- [ ] Unit/functional проверки controller-слоя на чтение/запись DID.
- [ ] Проверка сценария «нормальная подгонка 0%».
- [ ] Проверка ошибок связи/таймаутов.
- [ ] Проверка UI на разных размерах окна (без разъезда компонентов).
- [ ] Проверка кодировки UTF-8 во всех новых строках и файлах.

---

## Критерии готовности
- [ ] Оператор может подогнать 0% в 3-4 действия без ручной математики.
- [ ] Коррекция меняет только `zero_trim`.
- [ ] После записи и верификации значение стабильно в допуске.
- [ ] Интерфейс понятный, без перегруженности и без блокировок UI.
