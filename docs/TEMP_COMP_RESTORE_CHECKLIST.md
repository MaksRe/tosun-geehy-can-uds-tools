# Чек-лист восстановления и доработок температурной компенсации

Статус: рабочий документ.  
Правило работы: двигаемся строго сверху вниз, после каждого пункта отмечаем выполнение.

## Этап 1. База офлайн-анализа (CSV/all_nodes/XLSX)
- [x] Загрузка `CSV` и `XLSX` в одном потоке анализа.
- [x] Поддержка `all_nodes.csv`/`all_nodes.xlsx` с разбором нескольких узлов.
- [x] Селектор наборов данных внутри блока температурной компенсации (`Набор CSV/XLSX: 0xNN`).
- [x] Пересчет при загрузке нового файла запускается через `_apply_calibration_temp_comp_node_samples()` и `_recompute_calibration_temp_comp_metrics()` (логика подтверждена по коду).
- [x] Верхний селектор узла калибровки не пополняется офлайн-узлами из `CSV/XLSX` (используются отдельные dataset-опции temp-comp).

## Этап 2. Чтение/запись параметров МК
- [x] Кнопка чтения из блока температурной компенсации переведена на режимное чтение (`Чтение из текущего режима`).
- [x] Очередность чтения DID подтверждена в коде: следующий запрос отправляется только после завершения inflight/таймаута.
- [x] Таймауты/задержки чтения DID подтверждены в коде: используются delay/timeout таймеры последовательного чтения.
- [x] Чтение параметров ограничено активным режимом компенсации: используется mode-aware очередь DID.
- [x] «Записать рекомендации» пишет `K1/K0` и рассчитанные расширенные DID (`adv:*`) по очереди.
- [x] Отдельное чтение/запись `K0/K1` в расширенном блоке присутствует и подключено.

## Этап 3. Алгоритм и пересчет метрик
- [x] Технически исправлен пересчет при смене режима `0/1/2`: при ручной смене mode отключается линейное превью, чтобы оно не фиксировало `mode=0`.
- [x] Устранена типовая причина «режим 2 как режим 0»: при выборе mode=1/2 сохраняется осмысленный сегментный preview-профиль вместо вырожденного linear.
- [x] При ухудшении рекомендаций срабатывает guard и в статус добавляется явное предупреждение о сохранении текущих параметров.
- [x] Включены маркеры графика в рабочем лимите (`maxMarkerPoints=1600`) для видимости экстремумов без чрезмерной нагрузки.
- [x] Оффлайн-проверка кейсов `0x8A` и `0x6A` выполнена через `AppController`: метрики считаются и отдаются консистентно для выбранного dataset.

## Этап 4. UI/UX калибровки
- [x] Спойлер «Итог офлайн-анализа» есть и по умолчанию свернут.
- [x] Спойлер «Расширенные настройки (DID 0x001D..0x002C)» есть.
- [x] Блок 0%/100% вынесен в отдельный спойлер и больше не растягивается при сворачивании temp-comp.
- [x] График зафиксирован по высоте (без циклической привязки к Flickable), чтобы не уезжал за нижнюю границу.
- [x] Блок «Итог офлайн-анализа» упрощен: статус переведен в компактную строку.
- [x] Блок «Быстрое превью линейного режима» вынесен в отдельный спойлер и свернут по умолчанию.
- [x] Выравнивание и высота полей/подписей/кнопок в расширенных DID уплотнены и выровнены.
- [x] Цветовая схема кнопок в калибровке приглушена (убран контрастный фиолетовый акцент восстановления).

## Этап 5. Производительность и стабильность
- [x] Убраны лишние каскадные перерисовки графика: добавлен batched repaint (coalescing `requestPaint`) в `TrendCanvas`.
- [x] Ускорено переключение чекбоксов серий: добавлена защита от лишней пересборки/перерисовки по `chartRevision` и маске фильтра.
- [x] Прогресс-индикаторы и статус проверены для: загрузки файла, чтения из МК по очереди DID, пересчета и превью графиков.
- [x] Оффлайн-стресс на больших логах пройден: загрузка/пересчет для `all_nodes.csv` и `0x6A.csv` выполняются стабильно без ошибок парсинга/пересчета.

## Этап 6. Валидация на стенде и на ваших логах
- [x] Оффлайн-прогон на `all_nodes` из `18.03.2026_10-33-10` выполнен (диапазон температур для dataset `0x8A`: `-37.2..80.0 °C`).
- [x] Оффлайн-прогон на `0x6A.csv` из `19.03.2026_11-49-19` выполнен (диапазон температур: `-34.1..78.5 °C`).
- [ ] Сверка: рекомендации офлайн и фактическое поведение МК в колонке «Топливо».
- [ ] Зафиксировать список расхождений «ожидание/факт» и правила применения (когда сначала K1, когда затем подгонять K0 по текущей точке).

## Примечания по дисциплине изменений
- Не использовать полное восстановление файлов через `git restore`/`git checkout --` без прямого запроса.
- Вносить только точечные патчи с проверкой diff после каждого шага.
